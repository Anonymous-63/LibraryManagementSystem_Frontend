export class sidebar {
    constructor(title, route, icon, component) {
        this.title = title,
            this.route = route,
            this.icon = icon,
            this.component = component
    }
}